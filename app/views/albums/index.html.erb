<div class="mt-3">
  <% @albums.each do |album| %>
    <%= image_tag album.cover_photo_url(:thumbnail), size: "300x30", class: "img-thumbnail mr-2" %>
  <% end %>

<%= link_to '+ novo album', new_user_event_album_path, class: 'btn link ml-1' %>
</div>
